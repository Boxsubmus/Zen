using System;

load <std/math.zen>;

fun void test()
{
    return;
} // Compiler error, doesn't need to exist.

public fun void Main()
{
    println("hello, world!");
    println(scope $"2 + 2 = {add<int>(2, 2)}");
}